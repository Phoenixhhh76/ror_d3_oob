# OOB：Ruby 物件導向（OOP）速查筆記（對照 ex00 ~ ex05）

這份文件用你的專案內容當例子，解釋：

- `@name`（實例變數）是什麼、何時用
- `attr_reader` / `attr_writer` / `attr_accessor`（你寫的「assocer」通常是指 accessor）怎麼用、等價寫法是什麼
- Ruby 的繼承怎麼寫（`<`、`super`、覆寫）
- ex00 ~ ex05 每題應該學到哪些核心觀念

---

## 1) `@name` 是什麼？（Instance Variable / 實例變數）

### 1.1 定義

在 Ruby 裡，以 `@` 開頭的變數是 **實例變數（instance variable）**：

- **屬於某個物件（instance）本身**
- **在同一個物件的所有方法中都可讀寫**
- **用來保存「物件的狀態（state）」**

例如你在 ex00 的 `Html` 類裡會看到：

- `@page_name`：這個 Html 物件代表哪個頁面/檔名
- `@file`：目前開著的檔案 handle（File 物件）

### 1.2 作用域（scope）對比：`name` vs `@name`

```ruby
def initialize(name)
  name = name.upcase  # 這是「區域變數」，只在這個方法內有效
  @name = name        # 這是「實例變數」，會跟著物件存在
end
```

- **`name`（沒 @）**：區域變數，方法跑完就不見
- **`@name`**：儲存在物件內，之後其他方法也用得到

### 1.3 `@` 家族補充（知道即可）

- `@@x`：類別變數（class variable），整個類別共用（常見但也常踩雷，初學先少用）
- `$x`：全域變數（global），任何地方都能用（通常不建議）

---

## 2) `attr_reader` / `attr_writer` / `attr_accessor` 是什麼？

Ruby 走「資料封裝」的習慣：實例變數（`@name`）**預設不能直接從外面存取**。如果你想讓外部能「讀」或「寫」，就要提供方法（getter/setter）。

### 2.1 `attr_reader`（只讀 getter）

```ruby
class User
  attr_reader :name

  def initialize(name)
    @name = name
  end
end

u = User.new("Ada")
u.name        # => "Ada"
u.name = "X"  # => NoMethodError（沒有 setter）
```

**等價手寫：**

```ruby
def name
  @name
end
```

### 2.2 `attr_writer`（只寫 setter）

```ruby
class User
  attr_writer :name
end

u = User.new
u.name = "Ada"  # OK
u.name          # => NoMethodError（沒有 getter）
```

**等價手寫：**

```ruby
def name=(value)
  @name = value
end
```

### 2.3 `attr_accessor`（可讀可寫）

```ruby
class User
  attr_accessor :name
end

u = User.new
u.name = "Ada"
u.name # => "Ada"
```

= `attr_reader + attr_writer` 的組合。

### 2.4 什麼時候用哪個？

- **只想公開讀取，不想讓外部亂改**：用 `attr_reader`
  - 例如 ex00 的 `@page_name`：你通常希望「外部可以看」但「不要隨便改」
- **需要外部設定，但不想公開讀取**：用 `attr_writer`（較少）
- **兩者都需要**：用 `attr_accessor`

### 2.5 小陷阱：`attr_reader` 不代表「真的不可變」

就算你只給 `attr_reader`，如果回傳的是可變物件（例如 Array/Hash），外部仍可修改內容：

```ruby
class Box
  attr_reader :items
  def initialize
    @items = []
  end
end

b = Box.new
b.items << 1  # 仍然改得到 Array 內容
```

如果你真的要保護內部狀態，常見做法是回傳副本（`dup`）或凍結（`freeze`）。

---

## 3) Ruby 的繼承怎麼用？（Inheritance）

### 3.1 基本語法：`class 子類 < 父類`

```ruby
class Animal
  def speak
    "..."
  end
end

class Dog < Animal
  def speak
    "woof"
  end
end
```

- `Dog` 會「繼承」`Animal` 的方法與行為
- 子類可以 **覆寫（override）** 父類方法（如 `speak`）

### 3.2 `super`：呼叫父類同名方法（最常用在 initialize）

```ruby
class Parent
  def initialize(x)
    @x = x
  end
end

class Child < Parent
  def initialize(x, y)
    super(x)   # 呼叫 Parent#initialize
    @y = y
  end
end
```

`super` 的幾種常見用法：

- `super`：把「目前方法收到的參數」原封不動傳給父類
- `super(a, b)`：自己指定要傳什麼參數
- `super()`：傳「空參數」（即使目前方法有參數也不傳）

### 3.3 對照你的專案：ex04 用繼承做「語法糖」

ex03 你需要自己寫：

```ruby
Elem.new('html', [...])
```

ex04 你會做出：

```ruby
class Html < Elem
  def initialize(content = [])
    super('Html', content)
  end
end
```

這樣使用者就能寫 `Html.new([...])`，更直覺、也更不容易打錯 tag。

---

## 4) ex00 ~ ex05 應該學到的東西（重點整理）

下面以「你寫完這題後，腦中應該多了什麼能力」來總結。

### ex00：HTML（最基本的類 + 檔案輸出）

- **類別與物件**
  - `class Html`、`initialize`（建構子）
  - 用實例變數保存狀態：`@page_name`、`@file`
- **封裝與讀取器**
  - 用 `attr_reader` 讓外部能讀 `page_name`
- **檔案操作（寫入）**
  - `File.open(..., 'w')` + `puts` 逐行寫入
  - 最後要 `close`（或在 `finish` 內關閉）
- **流程設計**
  - `initialize` 內呼叫 `head`，建立基本骨架
  - `dump` 只負責追加內容
  - `finish` 收尾補上 `</body></html>`

### ex01：Raise HTML（用例外防止錯誤狀態）

- **例外處理的基本觀念**
  - 用 `raise "message"` 阻止「不合理操作」
- **狀態管理**
  - 用布林實例變數追蹤狀態（例如 body 是否已開/已關）
- **防禦性程式設計**
  - 做事前檢查：不符合狀態就拒絕執行（直接 raise）

### ex02：Rescue HTML（自訂例外 + 自動修復，讓程式不中斷）

- **自訂例外類**
  - `class Dup_file < StandardError` 這類繼承結構
- **例外不只是報錯，也可以是「修復流程」**
  - `show_state`（顯示現況）→ `correct`（修正）→ `explain`（說明結果）
- **更進階的檔案處理**
  - 修改檔名避免衝突（`.new` 插入副檔名前）
  - 在檔案中搬移 `</body>` 並插入文字，再維持正確結構

### ex03：Elem（用物件樹表示 HTML）

- **用物件表示 DOM 樹**
  - `Elem`（節點/標籤）包含 `content`
  - `Text` 表示純文字
  - `content` 可以裝 `Elem`（巢狀）或 `Text`
- **多型與覆寫**
  - 覆寫 `to_s`：物件如何「渲染」成字串
- **資料結構與相容性**
  - Array/Hash 的操作（`add_content`、`opt`）
  - 配合測試：單一 Text vs 多個 children 的不同輸出

### ex04：Dejavu（繼承 + 代碼重用）

- **大量小類別繼承自 `Elem`**
  - 讓使用更直覺：`Html.new`、`Body.new`、`H1.new`、`Img.new`...
- **`super` 的實戰**
  - 子類 initialize 只收必要參數，其他交給父類處理
- **更好的 API 設計**
  - 把常見預設值（tag 名、simple/double）藏在子類裡，降低使用者負擔

### ex05：Validation（建立規則系統 + 遞迴驗證）

- **設計一個「規則驗證器」**
  - `Page#is_valid?` 做為對外入口
  - 內部用 helper method 分拆規則
- **樹狀結構遞迴（DFS）**
  - 驗證節點 → 再驗證 children
- **白名單/結構約束**
  - 哪些節點可以出現
  - Head/Body 的順序與數量
  - 某些節點只能含 Text、或至少要有 Li/Tr 等

---

## 5) 建議你用這份筆記的方式

- 當你看到 `@xxx`：先問「這是不是物件狀態？哪些方法會用到？」
- 當你想把 `@xxx` 給外界用：用 `attr_reader/writer/accessor`，並思考「要不要讓外界改？」
- 當你在 ex04/ex05 寫很多相似類：先想「能不能用繼承 + super 把重複收掉？」
- 當你在 ex05 檢查結構：把資料想成「樹」，然後用遞迴逐層檢查規則

