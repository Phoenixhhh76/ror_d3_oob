# ROR_0_Oop 專案總結

## 專案概述

**ROR_0_Oop** 是 Ruby on Rails 訓練課程的面向對象編程模組，通過 6 個循序漸進的練習題，學習如何使用 Ruby 的面向對象特性來構建 HTML 生成和驗證系統。

---

## 專案結構

```
Ror_0_Oop/
├── ex00/  # HTML 基礎生成
├── ex01/  # 錯誤處理（異常）
├── ex02/  # 智能異常處理（自定義異常類）
├── ex03/  # 面向對象 HTML 元素（Elem 和 Text 類）
├── ex04/  # 繼承與代碼重用（HTML 元素類）
└── ex05/  # HTML 結構驗證系統
```

---

## 學校要求學習的內容

### 1. Ruby 基礎語法
- **Shebang 和警告標誌**：`#!/usr/bin/env ruby`、`# frozen_string_literal: true`
- **變量和常量**：實例變量 `@variable`、類變量 `@@variable`
- **方法定義**：`def method_name`、參數默認值
- **條件語句**：`if/unless`、`case/when`
- **迭代器**：`each`、`map`、`times`（禁止使用 `for`、`while`、`until`）

### 2. 面向對象編程（OOP）
- **類和對象**：`class ClassName`、`def initialize`
- **繼承**：`class Child < Parent`、`super` 方法調用
- **封裝**：`attr_reader`、`attr_writer`、`attr_accessor`
- **多態**：方法重載、`to_s` 方法重寫
- **訪問控制**：`public`、`private`、`protected`

### 3. 異常處理
- **異常類**：`StandardError`、自定義異常類
- **異常拋出**：`raise`、`raise ExceptionClass.new("message")`
- **異常捕獲**：`begin/rescue/ensure/end`
- **異常恢復**：智能異常處理、自動修正錯誤

### 4. 文件操作
- **文件讀寫**：`File.open`、`File.read`、`File.write`
- **文件模式**：`'r'`（讀）、`'w'`（寫）、`'a'`（追加）
- **文件檢查**：`File.exist?`、`File.absolute_path`
- **目錄操作**：`Dir.pwd`、`Dir.glob`

### 5. 數據結構
- **數組（Array）**：`[]`、`<<`、`concat`、`each`、`map`
- **哈希（Hash）**：`{}`、`key => value`、`each`、`key?`
- **字串處理**：`split`、`join`、`strip`、`chomp`、`gsub`
- **字串插值**：`"#{variable}"` vs `'#{variable}'`

### 6. HTML 知識
- **HTML 結構**：`<!DOCTYPE html>`、`<html>`、`<head>`、`<body>`
- **標籤類型**：雙標籤（`<div></div>`）、自閉合標籤（`<img />`）
- **HTML 屬性**：`<tag attr='value'>`
- **嵌套結構**：HTML 元素的層級關係

### 7. 設計模式
- **建造者模式（Builder Pattern）**：ex00-ex02 的 Html 類
- **策略模式（Strategy Pattern）**：不同的驗證規則
- **訪問者模式（Visitor Pattern）**：ex05 的驗證系統
- **模板方法模式（Template Method）**：Elem 類的 to_s 方法

### 8. 測試和驗證
- **單元測試**：使用 `test/unit`
- **驗證邏輯**：遞歸遍歷、規則驗證
- **錯誤報告**：清晰的錯誤信息和調試輸出

---

## 各練習題學習重點

### Exercise 00: HTML
**學習目標**：
- 理解類的基本結構
- 學習文件操作
- 理解方法調用順序

**核心知識點**：
- 類定義和實例化
- 實例變量 `@variable`
- `attr_reader` 訪問器
- 文件寫入操作

---

### Exercise 01: Raise HTML
**學習目標**：
- 理解異常處理機制
- 學習防禦性編程
- 掌握狀態追蹤

**核心知識點**：
- `raise` 拋出異常
- 狀態變量管理（`@body_opened`、`@body_closed`）
- 條件檢查和錯誤預防
- 異常信息格式

---

### Exercise 02: Rescue HTML
**學習目標**：
- 理解自定義異常類
- 學習異常恢復機制
- 掌握智能錯誤處理

**核心知識點**：
- 繼承 `StandardError`
- 異常類的三個方法：`show_state`、`correct`、`explain`
- 文件內容修改
- 錯誤自動修正

---

### Exercise 03: Elem
**學習目標**：
- 理解面向對象設計
- 學習方法重載（`to_s`）
- 掌握遞歸處理

**核心知識點**：
- `to_s` 方法重寫
- 遞歸調用
- 字串構建（`String.new` 處理 frozen string）
- 內容類型處理（Text vs Elem）

---

### Exercise 04: Dejavu
**學習目標**：
- 理解類繼承
- 學習代碼重用
- 掌握 `super` 方法

**核心知識點**：
- 類繼承 `class Child < Parent`
- `super` 調用父類方法
- 構造函數簡化
- 字串自動轉換為 Text

---

### Exercise 05: Validation
**學習目標**：
- 理解驗證系統設計
- 學習樹狀結構遍歷
- 掌握複雜規則實現

**核心知識點**：
- 遞歸遍歷（深度優先搜索）
- 規則驗證邏輯
- 錯誤報告機制
- 狀態管理

---

## 必須注意的細節

### 1. 代碼規範要求

#### Shebang 和警告標誌
```ruby
#!/usr/bin/env ruby
# frozen_string_literal: true
# warn_indent: true
```
- **必須包含**：所有提交的文件都必須有 shebang
- **警告標誌**：`frozen_string_literal` 會凍結所有字串字面量

#### 代碼組織
- **禁止全局作用域代碼**：所有代碼必須在類或函數中
- **測試代碼**：必須包含在 `if $PROGRAM_NAME == __FILE__` 區塊中
- **互動式測試**：必須能在 irb 中使用 `require_relative`

#### 循環限制
- **嚴格禁止**：`for`、`while`、`until` 關鍵字
- **違規後果**：使用這些關鍵字會被視為作弊，分數為 -42
- **替代方案**：使用 `each`、`map`、`times`、`loop` 等迭代器

#### 導入限制
- **禁止 require**：除非在「Authorized function」部分明確允許
- **允許的情況**：ex04 和 ex05 可以使用 `require_relative` 載入必要的類

---

### 2. 文件操作細節

#### 文件模式
- `'r'`：只讀模式
- `'w'`：寫入模式（會覆蓋現有文件）
- `'a'`：追加模式

#### 文件資源管理
- 使用 `File.open` 的區塊語法自動關閉文件
- 手動關閉：`file.close`
- 檢查文件狀態：`file.closed?`

#### 路徑處理
- `File.absolute_path`：獲取絕對路徑
- `File.basename`：獲取文件名
- `File.dirname`：獲取目錄路徑
- `File.join`：組合路徑

---

### 3. 異常處理細節

#### 異常類型
- `StandardError`：所有標準異常的基類
- 自定義異常：繼承自 `StandardError`
- 異常信息：必須精確匹配題目要求

#### 異常處理流程
```ruby
begin
  # 可能出錯的代碼
  raise MyError.new("message")
rescue MyError => e
  # 處理異常
  e.show_state
  e.correct
  e.explain
ensure
  # 清理代碼（可選）
end
```

#### 狀態管理
- 使用實例變量追蹤狀態
- 在適當的時機更新狀態
- 確保狀態一致性

---

### 4. 面向對象細節

#### 繼承
- 使用 `< Parent` 語法
- `super` 調用父類方法
- 可以重寫父類方法

#### 方法重載
- `to_s`：對象的字串表示
- `initialize`：構造函數
- 可以根據參數類型進行不同處理

#### 訪問控制
- `public`：公開方法（默認）
- `private`：私有方法（只能在類內部調用）
- `protected`：受保護方法（可以在子類中調用）

---

### 5. 字串處理細節

#### Frozen String
- `frozen_string_literal: true` 會凍結所有字串字面量
- 解決方案：使用 `String.new('')` 創建可變字串
- 或使用 `+` 運算符連接字串

#### 字串插值
- 雙引號 `""`：支持插值 `"#{var}"`
- 單引號 `''`：不支持插值，保持字面量

#### 字串方法
- `split("\n")`：按換行符分割
- `join("\n")`：用換行符連接
- `strip`：移除首尾空白
- `chomp`：移除末尾換行符

---

### 6. HTML 生成細節

#### 標籤格式
- 雙標籤：`<tag>content</tag>`
- 自閉合標籤：`<tag attr='value' />`
- 屬性格式：`key='value'`（使用單引號）

#### 換行處理
- 空標籤：`<tag>\n</tag>`
- 單個 Text：`<tag>Text</tag>`（無換行）
- 多個內容：每個內容後換行

#### 嵌套結構
- 正確的標籤嵌套順序
- 遞歸處理子元素
- 保持 HTML 結構完整性

---

### 7. 驗證系統細節

#### 遞歸遍歷
- 深度優先搜索（DFS）
- 先驗證當前節點，再驗證子節點
- 使用遞歸調用 `validate_node`

#### 規則實現
- 每個規則有獨立的驗證函數
- 使用 `case/when` 根據節點類型分發
- 提供清晰的錯誤信息

#### 狀態追蹤
- 使用 `@valid` 追蹤整體驗證狀態
- 繼續驗證以發現所有錯誤
- 提供詳細的驗證過程輸出

---

## 學到的知識點總結

### 1. Ruby 語言特性

#### 動態類型
- Ruby 是動態類型語言，不需要聲明變量類型
- 類型在運行時確定
- 使用 `is_a?` 檢查類型

#### 塊（Block）和迭代器
- `each`、`map`、`times` 等迭代器
- 塊語法：`do |item| ... end` 或 `{ |item| ... }`
- 區塊自動處理資源（如文件關閉）

#### 方法鏈
- 可以連續調用多個方法
- 例如：`str.chomp.gsub(',', '').strip.to_i`

---

### 2. 面向對象編程概念

#### 封裝
- 將數據和方法封裝在類中
- 使用訪問器控制數據訪問
- 隱藏實現細節

#### 繼承
- 代碼重用：子類繼承父類的方法
- 方法重寫：子類可以重寫父類方法
- 多態：不同類可以實現相同的方法

#### 多態
- 不同對象對同一消息做出不同響應
- `to_s` 方法在不同類中有不同實現
- 驗證系統根據節點類型執行不同邏輯

---

### 3. 設計模式應用

#### 建造者模式（Builder Pattern）
- ex00-ex02：Html 類逐步構建 HTML 文件
- 分步驟添加內容
- 最後調用 `finish` 完成構建

#### 策略模式（Strategy Pattern）
- ex05：不同的驗證規則作為不同的策略
- 根據節點類型選擇不同的驗證策略
- 易於擴展新的驗證規則

#### 訪問者模式（Visitor Pattern）
- ex05：驗證系統訪問不同的節點類型
- 對不同類型的節點執行不同的操作
- 分離數據結構和操作邏輯

---

### 4. 數據結構處理

#### 樹狀結構
- HTML 文檔是樹狀結構
- 使用遞歸遍歷樹
- 理解節點、邊、根、葉子等概念

#### 數組和哈希
- 數組：有序集合，使用索引訪問
- 哈希：鍵值對集合，使用鍵訪問
- 理解何時使用數組，何時使用哈希

---

### 5. 錯誤處理策略

#### 防禦性編程
- 檢查輸入有效性
- 驗證狀態一致性
- 提前發現和報告錯誤

#### 異常處理
- 使用異常處理錯誤情況
- 提供清晰的錯誤信息
- 實現錯誤恢復機制

#### 智能錯誤處理
- 不僅報告錯誤，還能修正錯誤
- 自動恢復系統狀態
- 提供修正過程的解釋

---

### 6. 測試和驗證

#### 單元測試
- 使用測試框架驗證功能
- 編寫測試用例覆蓋各種情況
- 確保代碼正確性

#### 驗證邏輯
- 實現完整的驗證規則
- 提供詳細的驗證過程
- 幫助調試和問題定位

---

### 7. 代碼質量

#### 可讀性
- 清晰的變量命名
- 適當的代碼註釋
- 邏輯分離和模塊化

#### 可維護性
- 代碼重用（繼承）
- 易於擴展（設計模式）
- 清晰的結構組織

#### 可測試性
- 包含測試代碼
- 支持互動式測試
- 提供詳細的輸出信息

---

## 專案進階學習建議

### 1. 深入理解 OOP
- 學習更多設計模式
- 理解 SOLID 原則
- 掌握接口和抽象類的概念

### 2. 擴展驗證系統
- 添加更多驗證規則
- 實現驗證規則的配置化
- 支持自定義驗證規則

### 3. 性能優化
- 優化遞歸遍歷算法
- 減少不必要的字符串操作
- 使用更高效的數據結構

### 4. 測試覆蓋
- 增加邊緣情況測試
- 實現自動化測試
- 使用測試驅動開發（TDD）

---

## 常見錯誤和解決方案

### 1. Frozen String 錯誤
**錯誤**：`can't modify frozen String`
**解決**：使用 `String.new('')` 創建可變字串

### 2. 循環關鍵字使用
**錯誤**：使用 `for`、`while`、`until`
**解決**：使用 `each`、`map`、`times` 等迭代器

### 3. 文件未關閉
**錯誤**：文件資源洩漏
**解決**：使用區塊語法或手動關閉文件

### 4. 狀態不一致
**錯誤**：驗證狀態與實際狀態不符
**解決**：確保在正確的時機更新狀態變量

### 5. 類型檢查錯誤
**錯誤**：未正確檢查節點類型
**解決**：使用 `is_a?` 或 `class.name` 進行類型檢查

---

## 總結

**ROR_0_Oop** 專案是一個完整的面向對象編程學習路徑，從基礎的類定義到複雜的驗證系統，涵蓋了：

1. **Ruby 基礎**：語法、數據結構、文件操作
2. **面向對象**：類、繼承、多態、封裝
3. **異常處理**：異常類、錯誤恢復、智能處理
4. **設計模式**：建造者、策略、訪問者模式
5. **系統設計**：驗證系統、樹狀結構處理

通過這 6 個練習題，我們不僅學會了 Ruby 編程，更重要的是學會了如何：
- 設計清晰的類結構
- 實現可重用的代碼
- 處理複雜的業務邏輯
- 構建健壯的系統

這些技能不僅適用於 Ruby，也是所有面向對象編程語言的核心概念。

---

**完成日期**：2024年
**專案狀態**：✅ 已完成所有練習題
